#include<iostream>

#pragma once
struct Song{
    int id;
    std::string title;
    std::string artist;
    std::string album;
    int duration;
    //Song(int ID, std::string tit, std::string art,std::string al, int dua):id(ID), title(tit), artist(art), album(al),duration(dua){};
};